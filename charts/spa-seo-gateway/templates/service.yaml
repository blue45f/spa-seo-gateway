apiVersion: v1
kind: Service
metadata:
  name: {{ include "spa-seo-gateway.fullname" . }}
  labels:
    {{- include "spa-seo-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spa-seo-gateway.selectorLabels" . | nindent 4 }}
